# 4D Art - 4D 像素矩阵生成器 PRD

**状态**：Phase 1 实现完成
**创建时间**：2026-05-10
**文档版本**：v0.8
**最后更新**：2026-05-21

---

## 1. 产品概述与愿景

### 产品定位

4D Art 是一个创意生成工具，让用户通过 AI 生成独特的四维像素艺术作品。用户可以自由探索四维物体的不同切片，感受超越三维的视觉体验。

### 核心价值主张

- **突破维度限制** — 将抽象的四维概念转化为可交互的视觉体验
- **创意与技术的结合** — AI 生成 + 实时可视化，让艺术创作无边界
- **独一无二的所有权** — Content Hash 确保每件作品的唯一性

### 目标用户

- 数字艺术爱好者 — 追求独特、新颖的艺术形式
- 技术探索者 — 对四维几何、维度理论感兴趣
- NFT 收藏家 — 寻求稀缺性强的数字艺术品
- 创作者 — 希望生成独特的 4D 艺术用于个人或商业用途

### 成功愿景 (Phase 1 Goal)

在第一阶段，用户可以快速生成一个数学定义的 4D 物体，通过四象限切片控制探索不同切面视角，感受四维空间的神秘与美。整个过程无需安装任何插件，打开浏览器即可体验。

**Phase 1 已实现状态**：
- 6 种预设 4D 物体：Tesseract、4D Sphere、4D Octahedron、4D Dodecahedron、4D Icosahedron、4D Torus
- 四象限切片面板 UI（xyzw 四个独立控制）
- 默认 w=12 唯一切片，xyz 自由轴
- 像素点密度控制（6 档：1-6）
- 3D 视角可自由旋转、缩放
- Content Hash 生成（SHA-256 前 12 字符）
- 一键截图自动保存

**Phase 1 测试覆盖率**：
- 模块级覆盖率：100%（hash, generators, slice, controls, stateManager）
- 测试结果：12 测试套件全部通过，261 测试通过

**Phase 1 已修复的问题**：
- 切片提取：现已支持任意轴组合切片
- 多轴切片切换：现已支持切片数量调整和切片轴切换
- Dodecahedron/Icosahedron：已修复 SDF 实现
- Torus 颜色常量：已修复颜色数组访问问题
- 默认状态统一：xyzw 初始值统一为 12
- 像素点密度控制：支持 1D/2D/3D 切片坐标映射

---

## 2. 用户故事与使用流程

### 用户故事

#### US-001: 生成 4D 艺术作品 (P0)
作为用户，我希望通过简单的操作生成 4D 艺术作品，以便快速体验四维视觉的魅力
- 选择预设的四维物体（超立方体、4D 球体等）
- 点击生成，系统计算并渲染 4D 数据
- 获得唯一的 Content Hash 作为作品标识

#### US-002: 探索四维切片 (P0)
作为用户，我希望通过切片控制来探索四维物体不同位置的形态，以便深入理解四维结构
- 至少选择 1 个维度作为切片，切片值决定在该维度上的切面位置
- 剩余未选为切片的维度可以自由旋转/缩放，但移动其滑条不影响图形展示
- 移动切片值会改变 3D 图形的形态（图形变形）
- 例：w=12 切片 → XYZ 自由旋转；w=20 切片 → XYZ 图形变形

#### US-003: 多切片探索 (P0)
作为用户，我希望同时选择多个维度作为切片，以便观察更低维度的截面形态
- 可以选择 1-3 个维度作为切片
- 切片数量 = 4 - 剩余自由轴数
- 例：w=20 和 y=7 都是切片 → 展示 XZ 平面（可自由旋转）

#### US-004: 调整像素点密度 (P0)
作为用户，我希望调整像素点的疏密程度，以便获得不同的视觉效果
- 选择密度等级 1-6
- Level 1 = 默认密度，Level 2-6 = 递增的稀疏程度（2x, 3x, 4x, 5x, 6x）

#### US-005: 提交作品参加展示 (P1)
作为用户，我希望将我满意的 4D 作品提交到前端展示区，以便分享给更多人欣赏

---

## 3. 功能详细规格

### Phase 1: 数学定义物体 · 纯前端实现

#### F-101: 4D 物体选择器 (P0)
用户可从预设的四维物体列表中选择要生成的对象

| 字段 | 值 |
|------|---|
| 输入 | 单选下拉菜单 |
| 选项 | 超立方体, 4D 球体, 4D 八面体, 4D 十二面体, 4D 二十面体, 4D 环面 |
| 默认值 | 超立方体 |

#### F-102: 截图按钮 (P0)
一键将当前 3D 视角截图并自动保存到本地

| 字段 | 值 |
|------|---|
| 截图格式 | PNG |
| 保存方式 | 点击按钮自动下载到本地 |
| 文件命名 | `4d-art-{timestamp}.png` |

#### F-103: 4D 数据生成器 (P0)
根据选定的物体类型和参数，在前端计算生成 4D 像素矩阵

| 字段 | 值 |
|------|---|
| 输出格式 | Float32Array [w][z][y][x][rgba] |
| 数据量 | 24³ = 13,824 点 ≈ 50KB |
| 计算时间 | < 2s |

#### F-104: 四象限切片控制 (P0)
管理 xyzw 四个维度的切片/自由状态，实时探索四维结构

| 字段 | 值 |
|------|---|
| 象限数量 | 4 个 (x, y, z, w) |
| 模式 | 切片 / 自由 (每个轴独立选择) |
| 切片数量 | 至少 1 个，最多 3 个 |
| 切片值范围 | [1, 24]，整数 |
| 默认状态 | w=12 作为唯一切片，xyz 为自由轴 |
| 帧率 | Target 60fps，16ms 内完成切片提取 |

**切片逻辑**：
- **切片**：该维度作为切片，其值决定切面位置
  - 移动切片滑条 → 图形形态改变（变形）
- **自由**：该维度不作为切片，可以旋转/缩放视图
  - 移动自由轴滑条 → 不影响图形展示，仅改变视角

**场景示例**：

| 切片轴 | 切片值 | 自由轴 | 3D 视图 |
|--------|-------|--------|---------|
| w | 12 | xyz | XYZ 自由旋转（图形不变形） |
| w | 20 | xyz | XYZ 图形变形 |
| w, y | 12, 7 | xz | XZ 平面（可自由旋转） |
| w, y, z | 12, 7, 15 | x | X 轴线条 |

**UI 组件**：
- 四轴控制面板，每个轴包含：轴标签、切片/自由切换按钮、滑条、数值显示
- 切片/自由切换：`[切片]` 或 `[自由]` 按钮
- 约束：至少 1 个切片轴

#### F-105: Three.js 3D 渲染器 (P0)
将当前切片数据渲染为可交互的视觉画面

| 字段 | 值 |
|------|---|
| 渲染引擎 | Three.js r158+, WebGL2, OrbitControls |
| 相机 | PerspectiveCamera，FOV 50° |
| 控制 | OrbitControls |
| 几何体 | Points / BoxGeometry 颗粒化 |
| 动态轴 | 根据固定状态动态计算参与渲染的轴 |

#### F-106: Content Hash 生成 (P1)
为每个生成的作品生成唯一的 Content Hash

| 字段 | 值 |
|------|---|
| 算法 | SHA-256，前 12 字符 |
| 格式 | 如: `a7f3b2c9d8e1` |

#### F-107: 像素点密度控制 (P0)
调整像素点之间的距离，使画面变得紧凑或稀疏

| 字段 | 值 |
|------|------|
| 密度等级 | 1, 2, 3, 4, 5, 6（共 6 档） |
| Level 1 | 默认间距（无缩放） |
| Level 2-6 | 默认距离的倍数（2x, 3x, 4x, 5x, 6x） |

**状态定义**：
- `pointSpacing`: number — 密度等级 [1-6]
- 默认值: 1

---

## 4. 坐标系定义

### 四维空间坐标系
四维物体存在于 xyzw 均为 [1, 24] 的空间里，索引从 1 开始。

### 中心点
所有四维物体的中心点均位于 [12.5, 12.5, 12.5, 12.5]（即空间中心）。

---

## 5. 技术架构

### 前端技术栈（Phase 1）

| 层级 | 技术 |
|------|------|
| UI Layer | HTML5 语义化标签, CSS 变量 (design tokens), Vanilla JS |
| Rendering Engine | Three.js r158+, WebGL2, OrbitControls |
| 4D Computation | TypedArrays (Float32Array) |

### 文件结构

```
4d-art/
├── index.html              # 单页面入口
├── css/
│   ├── tokens.css          # CSS 变量定义
│   ├── base.css            # 重置与基础样式
│   ├── components.css      # 组件样式
│   └── themes.css          # 主题样式
├── js/
│   ├── main.js             # 入口与初始化
│   ├── app.js              # 主应用逻辑
│   ├── fourD/
│   │   ├── generators.js   # 4D 物体数学定义
│   │   └── slice.js        # 切片提取
│   ├── render/
│   │   ├── scene.js        # Three.js 场景
│   │   ├── camera.js       # 相机控制
│   │   └── renderer.js     # WebGL 渲染器
│   ├── quadrant/
│   │   ├── stateManager.js # 四象限状态管理
│   │   └── controls.js     # 四象限 UI 控制
│   ├── ui/
│   │   └── state.js        # 应用状态管理
│   └── utils/
│       └── hash.js         # Content Hash 生成
```

---

## 6. 验收标准

### Phase 1 交付标准

#### 功能验收

- [x] 用户可选择至少 5 种预设四维物体并成功生成
- [x] 生成 24³ 分辨率 4D 数据时间 < 3 秒
- [x] 四象限控制响应时间 < 16ms（60fps）
- [x] 3D 视角可自由旋转、缩放，无卡顿
- [x] 每个作品生成唯一的 12 位 Content Hash
- [x] 截图功能保存当前视图为 PNG
- [x] xyzw 四个象限均显示独立的切片/自由切换按钮和滑条
- [x] 切片模式下滑条位置决定切面位置，移动滑条图形形态改变
- [x] 自由模式下滑条可拖动但不影响图形展示
- [x] 像素点密度控制 6 档可调

#### 性能验收

- [ ] 首屏加载时间 < 2 秒（3G 网络）
- [ ] 内存占用峰值 < 200MB

### Phase 1 功能清单

| 功能 ID | 功能名称 | 实现文件 | 状态 |
|--------|---------|---------|------|
| F-101 | 4D 物体选择器 | `index.html`, `js/main.js` | **已实现** |
| F-102 | 截图按钮 | `index.html`, `js/app.js` | **已实现** |
| F-103 | 4D 数据生成器 | `js/fourD/generators.js` | **已实现** |
| F-104 | 四象限切片控制 | `js/quadrant/controls.js`, `js/quadrant/stateManager.js`, `css/components.css`, `index.html` | **已实现** |
| F-105 | Three.js 3D 渲染器 | `js/render/scene.js`, `js/render/camera.js`, `js/render/renderer.js` | **已实现** |
| F-106 | Content Hash 生成 | `js/utils/hash.js`, `js/app.js` | **已实现** |
| F-107 | 像素点密度控制 | `js/app.js`, `js/ui/state.js` | **已实现** |

## 7. 代码提交

| Commit | 描述 |
|--------|------|
| `05a2b10` | fix: updateHashDisplay not accessible from main.js |
| `5a61035` | fix: startup issues - variable declaration and missing actions |
| `596b446` | fix: update hash display on shape/parameters change |
| `d2b280e` | feat: implement F-102 screenshot and F-106 content hash |
| `a3b13b5` | refactor: consolidate F-104/F-108 and add F-107 point spacing |

---

## 8. 附录：关键技术决策记录

### K-01: Content Hash vs 链上 NFT
Phase 1-2 使用 Content Hash（类似 Git commit），不上链。降低用户门槛，后续可升级绑定区块链。

### K-02: 切片/自由模式设计
切片决定切面位置，自由决定视角。概念清晰，交互直观。

### K-03: 像素点密度控制
通过缩放点坐标位置实现，无需修改几何体结构，实现简单高效。

### K-04: 中英文界面切换
使用语言切换按钮在中文/英文之间切换，所有 UI 文本通过 i18n 系统管理，提升国际化体验。
